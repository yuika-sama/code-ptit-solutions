#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<string.h>
#include<stdbool.h>
#define scan(n) scanf("%d",&n)
#define print(n) printf("%lld " , n)
#define linedown printf("\n")
main(){
	int t;
	scan(t);
	while (t--){
		int m,n;
		scan(m); scan(n);
		int a[1000][1000];
		for (int i=0; i<m; i++){
			for (int j=0; j<n; j++){
				scan(a[i][j]);
				getchar() - 48;
				
			}
		}
		int d=0;
		for (int i=0; i<m; i++){
			for (int j=0; j<n; j++){
				
			}
		}
	}
	
}
